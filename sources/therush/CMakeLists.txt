#-------------------------------------------------------------------
# This file is part of the CMake build system for GUCEF
#     (Galaxy Unlimited Framework)
# For the latest info, see http://www.VanvelzenSoftware.com/
#
# The contents of this file are placed in the public domain. Feel
# free to make use of it in any way you like.
#-------------------------------------------------------------------

#
# THIS FILE IS AUTOMATICALLY GENERATED BY CMAKELISTGENERATOR
#
# CMAKELISTGENERATOR was compiled on: May  6 2010 @ 23:48:34
#
#
#
# -- Basic CMake concepts:
#      CMAKE_MODULE_PATH
#      – Path to where the CMake modules are located
#      CMAKE_INSTALL_PREFIX
#      – Where to put files when calling 'make install'
#      CMAKE_BUILD_TYPE
#      – Type of build (Debug, Release, ...)
#      BUILD_SHARED_LIBS
#      – Switch between shared and static libraries

# Configure therush

set(HEADER_FILES 
  targetver.h
)

set(SOURCE_FILES 
  therush.cpp
)

# Make sure the PLATFORM_HEADER_FILES variable is always defined
 set( PLATFORM_HEADER_FILES "" )

# Make sure the PLATFORM_SOURCE_FILES variable is always defined
 set( PLATFORM_SOURCE_FILES "" )

# For ease of use make a variable that has all files for this module
set( ALL_FILES ${HEADER_FILES} ${SOURCE_FILES} ${PLATFORM_HEADER_FILES} ${PLATFORM_SOURCE_FILES} )




#******** END OF AUTOGENERATED CONTENT ********

add_definitions(
	-DTINYXML_USE_STL
	-DTIXML_USE_STL
)
add_executable(
    therush
	WIN32
    ${ALL_FILES}
)
find_library(MINIBALL_LIBRARY miniball PATHS ${CMAKE_SOURCE_DIR}/../libs)
#~ find_library(TINYXML_LIBRARY tinyxml PATHS ${CMAKE_SOURCE_DIR}/../libs )
find_library(RAKNET_LIBRARY RakNetDLL PATHS ${CMAKE_SOURCE_DIR}/../libs /local/lib/)
if (NOT MINIBALL_LIBRARY)
	message(FATAL_ERROR "MiniBall not found in ${CMAKE_SOURCE_DIR}/../libs ! Please compile it first!")
endif (NOT MINIBALL_LIBRARY)

#~ if (NOT TINYXML_LIBRARY)
#~ message(FATAL_ERROR "TinyXML not found !")
#~ endif (NOT TINYXML_LIBRARY)

if (NOT RAKNET_LIBRARY)
	message(FATAL_ERROR "RakNet not found !")
endif (NOT RAKNET_LIBRARY)

find_package(OpenGL)

message("miniball : ${MINIBALL_LIBRARY}")
#~ message("tinyxml : ${TINYXML_LIBRARY}")
message("raknet : ${RAKNET_LIBRARY}")

target_link_libraries(
    therush

    platform # needs CEGUI, FMOD
    game # needs raknet and world AFTER (and render ?)
    render
    world # needs librender AFTER

    #~ edit # needs base, openGL
	base # needs winmm

    #~ ${TINYXML_LIBRARY}
    tinyxml
    ${MINIBALL_LIBRARY}
	${RAKNET_LIBRARY}
	#~ raknet

    fmodex

    LinearMath
    GIMPACTUtils
    BulletCollision
    BulletDynamics
    BulletMultiThreaded
    BulletSoftBody
    CEGUIBase
    CEGUIOpenGLRenderer
    CEGUITinyXMLParser
    Cg
    CgGL
	${OPENGL_LIBS}
    pthread
)

if (WIN32)
	target_link_libraries(
		therush

    	CEGUIDirect3D9Renderer
		CgD3D9
		d3dx9
		mingw32 # needed only for SDL...
		sdlmain
		ws2_32 #for raknet
	)
endif (WIN32)

target_link_libraries(
	therush

	SDL #On windows this MUST be AFTER mingw32 and sdlmain
)
